/**
 * Created by vishuja
 */
(function (window, document, $, angular) {

    var expenseApp = angular.module('expenseApp');

    expenseApp.controller("expenseCtrl", ["$scope", "$rootScope", "expenseSvcs", "expenseModel", "lookupStoreSvcs", "$state", "notifySvcs", "localStorage", "http", "$timeout", "modalSvcs", "registrationSvcs", function ($scope, $rootScope, expenseSvcs, expenseModel, lookupStoreSvcs, $state, notifySvcs, localStorage, http, $timeout, modalSvcs, registrationSvcs) {

        var vm = this;
    }]);

})(window, window.document, window.jQuery, window.angular);